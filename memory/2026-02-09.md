# 2026-02-09

- Integrated analytics health checks with Swift app rebuild/recompute flow and exposed status/log + manual trigger in Analytics view.
- Calibrated sample smoke topic threshold to `ceil(0.4*N)` primary-topic size to avoid flaky 10-paper random failures.
- Verified end-to-end reliability with:
  - `swift test`
  - `.venv/bin/python -m pytest analytics/tests -v`
  - `.venv/bin/python -m ruff check scripts/topic_focus_audit.py analytics/tests/test_topic_focus_audit_unit.py`
  - `scripts/run_example_smoke.sh --count 10` (two random passes)
- Full corpus run request executed (all 115 PDFs from `examples/` into repo `Output/`):
  - Found and fixed a real defect: duplicate inferred titles could overwrite `.paper.json` files (`114/115` symptom).
  - Fix: id-suffixed paper JSON filenames + safe legacy migration only for matching paper id/filePath.
  - Re-ran ingest gate and got `115/115` JSON + MD/chunks checks passing.
  - Rebuilt analytics + ANN + audits on full corpus:
    - output artifact audit: 0 findings
    - topic reliability audit: PASS (8 reliable topics, largest topic size 27, silhouette 0.1321)
